import React from "react";
import { ButtonToolbar } from "react-bootstrap";
import { Button } from "react-bootstrap";

const Register = ( props ) => {
  return (
 	<ButtonToolbar>
		<Button bsStyle="primary" bsSize="large" id="loginButton">
		Sign Up
		</Button>
	</ButtonToolbar>
	
	);
  
 };

  
export default Register

